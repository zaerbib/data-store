import { UpdateRequestMiddleware } from './update-request.middleware';

describe('UpdateRequestMiddleware', () => {
  it('should be defined', () => {
    expect(new UpdateRequestMiddleware()).toBeDefined();
  });
});
